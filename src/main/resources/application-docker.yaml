spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
logging:
  level:
    com.innowise: ${APP_LOGGING_LEVEL:info}
eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/